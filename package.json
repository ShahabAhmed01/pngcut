{
  "name": "pngcut",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Free, private, in-browser background remover. No signup, no ads, no upload-based processing.",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@imgly/background-removal": "1.7.0",
    "onnxruntime-web": "1.21.0"
  },
  "devDependencies": {
    "vite": "^7.3.2",
    "vitest": "^3.1.1"
  }
}